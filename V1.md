# V1 — Implementation Log

Living doc for V1. Every checkpoint (see [ROADMAP.md](./ROADMAP.md) for the checklist) gets one section here, filled in **as it's built**, in this shape:

- **Questions asked & decisions** — anything that had more than one reasonable answer, what was chosen, and why
- **What was built** — the concrete files/features
- **Why** — the reasoning behind how it was built, not just what
- **How** — how to run/verify it
- **Notes / gotchas** — anything that cost time or is easy to get wrong again later

ARCHITECTURE.md is the *target* design; this file is the *actual* build record, checkpoint by checkpoint.

Written for a backend dev coming from Java/Spring Boot/MySQL, not from Python/JS — new tools get a plain-language explanation or a mapping to their closest Java/Spring/MySQL equivalent (see the table below) the first time they show up, instead of assumed knowledge.

## Rosetta stone (Java / Spring Boot / MySQL → this stack)

You already know backend systems — just not these specific tools. Same concepts, different names:

| You know | Here | Notes |
|---|---|---|
| Spring Boot app + embedded Tomcat | FastAPI + Uvicorn | `create_app()` in `main.py` is your `@SpringBootApplication` equivalent; Uvicorn is the server that runs it, like embedded Tomcat runs a Spring app |
| `@RestController` + `@GetMapping` | `APIRouter` + `@router.get(...)` | Same idea — a function decorated to handle one route, grouped into a router class |
| `application.properties` + `@ConfigurationProperties` | `.env` file + `pydantic-settings` `Settings` class | A typed config object that reads environment variables — same job |
| JPA / Hibernate | SQLAlchemy | The ORM. `Mapped[...]` + `mapped_column(...)` ≈ `@Entity` + `@Column`; `relationship(...)` ≈ `@OneToMany` / `@ManyToOne` |
| Flyway / Liquibase | Alembic | Generates versioned migration scripts from your ORM models instead of hand-writing SQL |
| MySQL | PostgreSQL | Same relational mental model. Postgres adds a native `JSONB` column type (queryable JSON in a column) — used here for LLM output like `missing_skills` |
| JDBC driver | `psycopg` | The actual driver SQLAlchemy talks through to reach Postgres |
| `pom.xml` / `build.gradle` | `requirements.txt` + a `venv` | Dependency list; `venv` is a project-local Python install so dependencies don't leak between projects |
| Thymeleaf/JSP (server renders HTML) | React (via Vite) | Now a separate app: the browser renders the UI and calls your API with `fetch`, instead of the server generating HTML pages |

---

## Checkpoint 0 — Repo & docs

### Questions asked & decisions

| Question | Decision | Why |
|---|---|---|
| Auth: hand-rolled JWT or Clerk? | Hand-rolled JWT (FastAPI + passlib + python-jose) | Stronger interview story ("I built auth") than wiring up a SaaS; keeps all user data in one Postgres DB instead of adding an external auth provider dependency |
| Job sourcing for V2: legit APIs or LinkedIn/Indeed/Naukri scraping? | Legit public job-board APIs (Adzuna, RemoteOK, Arbeitnow, USAJobs, Greenhouse/Lever) | LinkedIn and Indeed actively detect/block scrapers and it violates their ToS — the actual reason most beginner clones of this project stall on IP/account bans instead of shipping. Playwright automation against LinkedIn/Indeed is demoted to an optional, explicitly risk-flagged V3 stretch goal (own account only, manual confirm-before-submit, rate-limited) |
| Initialize git now? | Yes | Recruiters look at commit history; better to have real incremental history from day one than one giant "initial commit" later |
| Python tooling: venv+pip or `uv`? | venv + pip + `requirements.txt` | Matches what FastAPI tutorials use; one less new tool to learn while also learning FastAPI, Postgres, and the OpenAI API for the first time |

### What was built

`README.md`, `ARCHITECTURE.md`, `ROADMAP.md`, `.gitignore`, `.env.example`; git repo initialized with one commit (`509ec59`). No application code — deliberately docs/scaffolding only.

### Why

The original project idea (see chat history / README's "Why this project") was solid but risked the two classic beginner failure modes: (1) too much infrastructure before any feature works, and (2) scraping LinkedIn/Indeed directly, which tends to get accounts/IPs banned before V2 is even usable. Both were designed out before any code was written, and the plan was reviewed with the user (via plan mode) before committing to it.

Beyond the four questions above, the plan also made these unprompted improvements to the original sketch, recorded here since they explain *why* later checkpoints are shaped the way they are:
- **Auth moved into V1** (not deferred to V2/V3) — the DB schema is much easier to get right from day one than to retrofit foreign keys onto `users` after V2/V3 add saved jobs and application tracking.
- **DB schema for all of V1–V3 sketched upfront** (in ARCHITECTURE.md: `users`, `resumes`, `job_descriptions`, `matches`, `applications`) so each checkpoint slots into one coherent schema instead of ad-hoc tables.
- **OpenAI structured outputs** (`response_format={"type": "json_schema", ...}`) planned instead of prompting "return JSON" as free text — materially more reliable.
- **`gpt-4o-mini` as the default model**, configurable via env var, for cost control while iterating.
- **Folder structure**: `resume_parser/`, `ai_engine/`, `job_scraper/` nested under `backend/app/services/` (not top-level siblings of `backend/`) so they're normal importable Python submodules of one package.
- **Dockerfile deferred** to the deployment checkpoint — not needed for local dev with `uvicorn`/`vite`.

### How

```
git log --oneline    # 509ec59 Checkpoint 0: project docs and repo scaffolding
```
Read `README.md` (pitch/stack), `ARCHITECTURE.md` (schema/design), `ROADMAP.md` (checklist) directly.

### Notes / gotchas

None yet — this checkpoint was docs-only.

---

## Checkpoint 1 — Backend + frontend skeletons

### Questions asked & decisions

Two more architectural questions came up after Checkpoint 0 was reviewed, before writing any Checkpoint 1 code:

**Q: Next.js or plain React for the frontend?**
Decision: switched from the originally-planned Next.js to **React + Vite + React Router**.
Why: Next.js's main selling points (server-rendered pages, its own API routes) exist to let a frontend act as its own backend. This project already has a real backend (FastAPI), so none of that gets used — the frontend is just a browser app calling your API with `fetch`, same as a Thymeleaf app would call a `@RestController`, except now it's a separate project instead of the same one. Plain React + Vite is the simpler, more direct match for that setup, with no unused framework machinery to learn on top of everything else.

*New concepts here with no direct Java/Spring equivalent, in plain terms:*
- **Vite** — the dev server + build tool for the frontend. Roughly: what `spring-boot:run` + hot reload is for your Spring app, but for the React/TypeScript code.
- **React** — a UI library where the page is built out of small reusable functions ("components") that return HTML-like markup. Replaces what a Thymeleaf/JSP template would render, except it runs in the browser and re-renders itself when data changes, instead of the server generating a new HTML page per request.
- **React Router** — decides which component to show for which URL, entirely in the browser (no server round-trip per page). Same *purpose* as `@RequestMapping`, different *place* it runs.
- **Tailwind CSS** — instead of writing CSS classes in a separate stylesheet, you style elements by combining small utility classes directly in the markup (e.g. `class="text-center gap-4"`).

**Q: Why Postgres and not MongoDB/SQLite/MySQL?** *(re-confirmed, no change)*
- vs **MongoDB**: the schema (`users → resumes → job_descriptions → matches → applications`) is genuinely relational — every table has foreign keys into another, and V3's analytics dashboard needs real joins/aggregations. Postgres's `jsonb` columns already give schema-flexible storage for LLM output (`missing_skills`, `strengths`) *inside* a relational table, so there's no upside to a document DB here, only the cost of hand-rolling referential integrity.
- vs **SQLite**: fine for local dev, but most PaaS platforms (Railway/Render) wipe the filesystem on redeploy, so a file-based DB doesn't survive in production, and it doesn't handle concurrent multi-user writes.
- vs **MySQL**: roughly comparable, but Postgres has stronger JSON support and is the more common default in FastAPI + SQLAlchemy + Alembic tutorials.

### What was built

**Backend** (`backend/`)
- FastAPI app factory in `app/main.py`, CORS enabled for `http://localhost:5173` (Vite's default dev port)
- `app/config.py` — `pydantic-settings` `Settings` class, reads `backend/.env` (mirrors the vars in root `.env.example`: `DATABASE_URL`, `JWT_*`, `OPENAI_*`). Schema only — not consumed by any route yet, that starts at Checkpoint 2/3.
- `GET /health` route (`app/routes/health.py`) returning `{"status": "ok"}` — the only real endpoint so far.
- `tests/test_health.py` — one pytest smoke test via `TestClient`.
- `requirements.txt`: fastapi, uvicorn[standard], pydantic-settings, pytest, httpx

**Frontend** (`frontend/`)
- Scaffolded with `npm create vite@latest frontend -- --template react-ts` (React 19, Vite 8, TypeScript)
- Tailwind CSS v4 via the `@tailwindcss/vite` plugin
- React Router (`react-router-dom`); `src/router.tsx` → currently just `/` → `src/pages/Home.tsx`
- `src/lib/api.ts` — fetch wrapper reading `VITE_API_BASE_URL` (default `http://localhost:8000`)
- `Home.tsx` calls the backend `/health` endpoint and renders live status ("checking" / "ok" / "unreachable")
- Removed default Vite template cruft (`App.tsx`, `App.css`, `assets/`)

### Why

The point of this checkpoint wasn't features — it was proving the two halves of the stack actually boot and talk to each other before anything real (DB, auth, AI) gets layered on. That's why the only "feature" is a health check rendered end-to-end through the frontend rather than, say, a static landing page: it's a genuine integration smoke test (CORS config, env-based API URL, fetch call, live UI state) rather than two independent hello-worlds.

Tailwind was set up via its v4 Vite plugin (not the v3-style `postcss.config.js` + `tailwind.config.js`) because that's the current documented path for Tailwind v4 — using the old approach would have been outdated the moment it was written.

### How

```
# backend
cd backend
python -m venv venv
./venv/Scripts/activate   # or source venv/bin/activate on macOS/Linux
pip install -r requirements.txt
cp ../.env.example .env   # then fill in real values
uvicorn app.main:app --reload

# frontend (separate terminal)
cd frontend
npm install
cp .env.example .env
npm run dev
```

**Verification actually performed:**
- `pytest -q` in `backend/` → 1 passed
- `uvicorn app.main:app` boots; `curl localhost:8000/health` → `{"status":"ok"}`
- `npm run build` in `frontend/` → type-checks and builds cleanly
- Ran both dev servers simultaneously (`uvicorn` on :8000, `vite` on :5173) and confirmed `Home.tsx`'s live health-check renders `ok`, proving CORS + env-based API URL + fetch all work together

### Notes / gotchas

- Vite env files are per-package: `frontend/.env` (from `frontend/.env.example`), separate from the root `.env`/`.env.example` used by the backend. The root `.env.example` originally had a leftover `NEXT_PUBLIC_API_BASE_URL` from the pre-switch plan — removed; frontend now has its own `.env.example` with `VITE_API_BASE_URL`.
- `.gitignore`'s Node section still said `.next/` / `next-env.d.ts` from the original Next.js plan — replaced with `dist/` (Vite's build output) and `.vite/`.
- On this machine, Vite's dev server needed `--host 127.0.0.1` (or hitting `localhost` instead of `127.0.0.1`) to be reachable from the shell — a local loopback quirk, not a project issue.

---

## Checkpoint 2 — Postgres models + Alembic migrations

Done.

### Questions asked & decisions

**Q: How to run Postgres locally, since neither Postgres nor Docker is installed on this machine?**
Options considered: free-tier cloud Postgres (Neon/Supabase), native Windows Postgres install, or Docker just for a DB container.
Decision: **free-tier cloud Postgres (Neon)**.
Why: fastest path with no local install, and it doubles as the eventual deployment DB (ARCHITECTURE.md already planned on a managed Postgres for deployment) — one less thing to redo later.

**Getting the Neon connection string** (steps given to the user, to run themselves since it requires creating a personal account):
1. Go to neon.tech and sign up (GitHub login is fastest)
2. Create a project (e.g. name it `autopilotai`), any nearby region, default Postgres version
3. On the project dashboard's **Connection Details** panel, copy the connection string: `postgresql://<user>:<password>@<host>.neon.tech/<dbname>?sslmode=require`
4. Edit the scheme from `postgresql://` to `postgresql+psycopg://` (keep the rest, including `?sslmode=require`) — needed because this project uses the `psycopg` v3 driver, not the legacy `psycopg2` one
5. Paste the edited string into `backend/.env` as `DATABASE_URL` (already git-ignored)

Note: Neon's free tier auto-suspends its compute after inactivity and wakes on the next query with a ~1s cold start — expected, not a bug.

### What was built so far

Using the [Rosetta stone](#rosetta-stone-java--spring-boot--mysql--this-stack) mapping from the top of this doc: SQLAlchemy = your Hibernate/JPA, Alembic = your Flyway/Liquibase.

- `app/database.py` — SQLAlchemy `engine` + `SessionLocal` (≈ a JPA `EntityManagerFactory`/`SessionFactory` pair) and `Base` (the equivalent of Hibernate's entity registry — every model class below "registers" itself here)
- `app/models/user.py`, `resume.py`, `job_description.py`, `match.py` — one model per ARCHITECTURE.md's schema. Each is basically a JPA `@Entity` class: `Mapped[int] = mapped_column(primary_key=True)` ≈ `@Id`, `ForeignKey("users.id")` ≈ `@ManyToOne`/`@JoinColumn`, `relationship(back_populates=...)` ≈ the matching `@OneToMany` on the other side. `matches.missing_skills` / `strengths` use Postgres's `JSONB` column type (MySQL's `JSON` column, but natively queryable)
- `app/models/__init__.py` — imports all four models in one place purely so Alembic sees the complete schema when it scans for changes (if a model file is never imported anywhere, Alembic won't know it exists — same gotcha as forgetting to add an `@Entity` to your persistence unit)
- `alembic/` scaffolded via `alembic init alembic`; `alembic/env.py` edited to pull the DB URL from `app.config.settings` (the same config object the whole app uses) instead of a second hardcoded URL in `alembic.ini`
- Added `sqlalchemy`, `alembic`, `psycopg[binary]` to `requirements.txt` (`psycopg` is the JDBC-driver equivalent — the actual thing that talks to Postgres over the wire)

### Why

Models use SQLAlchemy 2.0's newer typed style (`Mapped[...]`) rather than its older, more dynamic style — it's the current documented approach and gives real type checking on fields, closer to how you'd expect a typed `@Entity` to behave.

Worth calling out since it's a common point of confusion coming from Hibernate: Alembic's `--autogenerate` is **not** like Hibernate's `ddl-auto=update`. It doesn't touch the database itself — it *compares* your model classes against the current DB schema and writes out a plain Python/SQL migration **file** for you to read and run explicitly (`alembic upgrade head`). Nothing changes in the database until that second step. That's why the migration was reviewed by hand before applying it (see below) — same spirit as reviewing a Flyway migration script before merging it, except Alembic drafts the first version for you.

### How

```
cd backend
# real DATABASE_URL already in .env (Neon, postgresql+psycopg:// scheme)
alembic revision --autogenerate -m "create users, resumes, job_descriptions, matches tables"
alembic upgrade head
```

**Verification actually performed:**
- Tested the live connection directly (`engine.connect()`) before generating anything → connected OK
- `alembic revision --autogenerate` → correctly detected all 4 new tables, the `ix_users_email` unique index, all 3 foreign keys, and both `JSONB` columns; migration reviewed by hand before applying (file: `alembic/versions/7f0833c7b729_create_users_resumes_job_descriptions_.py`)
- `alembic upgrade head` → applied cleanly
- `inspect(engine).get_table_names()` against the live Neon DB → `['alembic_version', 'job_descriptions', 'matches', 'resumes', 'users']`, confirming the migration actually landed, not just that it ran without error
- `pytest -q` → still 1 passed (no regressions)

### Notes / gotchas

- Installed `psycopg` (v3) instead of the older `psycopg2`. SQLAlchemy defaults a bare `postgresql://` URL to `psycopg2`, which isn't installed — hit a `ModuleNotFoundError: No module named 'psycopg2'` on first import. Fixed by using the `postgresql+psycopg://` scheme everywhere (root `.env.example` and `backend/.env`) to explicitly select the v3 driver.
- Verified models at least *import* and register correctly without a live DB (`Base.metadata.tables.keys()` → all four tables) — `create_engine()` doesn't connect eagerly, so this worked even before the real `DATABASE_URL` existed.
- Neon's connection string as copied from the dashboard uses `postgresql://` — remember to rewrite it to `postgresql+psycopg://` every time a fresh one is issued (e.g. if the DB is ever recreated).

---

## Checkpoint 3 — JWT auth (signup / login / me)

Done.

### Questions asked & decisions

**Q: `passlib` + `python-jose` (as originally planned in ARCHITECTURE.md) or something else?**
Decision: swapped to **`bcrypt`** (used directly, no wrapper library) + **`PyJWT`**.
Why: tested both first. `passlib`'s bcrypt integration has a known compatibility issue with recent `bcrypt` versions (it reads an internal version attribute that newer `bcrypt` releases removed), which throws a warning on every hash/verify call — not a hard failure, but not something a portfolio project should ship with a warning wallpapering every request. `PyJWT` is the more actively maintained of the two JWT libraries and has a simpler API for the same job. Functionally this is a drop-in swap — same job (hash passwords, sign/verify tokens), no impact on anything else in ARCHITECTURE.md.

**Q: Where does the "DTO" layer live?**
Decision: added `app/schemas/` (not in the original ARCHITECTURE.md folder list) for Pydantic request/response models (`UserCreate`, `UserLogin`, `UserOut`, `Token`), separate from `app/models/` (the SQLAlchemy/DB models).
Why: this is exactly the same reason a Spring Boot project keeps DTOs separate from `@Entity` classes — you don't want to accidentally expose `hashed_password` in an API response just because it's a field on the DB model. `UserOut` explicitly whitelists which fields leave the API. ARCHITECTURE.md updated to include this folder.

### What was built

Using the Rosetta stone: FastAPI's `Depends(...)` is roughly Spring's `@Autowired`/constructor injection — instead of a DI container wiring beans, each route function declares what it needs as a parameter, and FastAPI resolves it per-request.

- `app/database.py` — added `get_db()`, a generator that hands out one SQLAlchemy session per request and closes it afterward (≈ opening one Hibernate `Session` per request and closing it at the end — the usual request-scoped session pattern)
- `app/auth/security.py` — `hash_password` / `verify_password` (bcrypt), `create_access_token` / `decode_access_token` (PyJWT, `HS256`, expiry from `settings.access_token_expire_minutes`)
- `app/auth/dependencies.py` — `get_current_user`: pulls the `Authorization: Bearer <token>` header (via FastAPI's built-in `OAuth2PasswordBearer`), decodes it, loads the matching `User` row, or raises `401`. This is the FastAPI equivalent of a Spring Security filter that populates the security context from a JWT — except here it's just a function any route can ask for via `Depends(get_current_user)`, no filter chain/global config involved
- `app/schemas/user.py` — `UserCreate`, `UserLogin`, `UserOut`, `Token` (the DTOs, see above)
- `app/routes/auth.py` — `POST /auth/signup`, `POST /auth/login`, `GET /auth/me`
- `tests/test_auth.py` — 4 tests: full signup→login→me flow, duplicate email rejected, wrong password rejected, missing token rejected. Each test generates a unique email (`uuid4`) to avoid colliding with previous runs, since these tests run against the real dev Neon DB rather than a separate test database
- Generated a real `JWT_SECRET` (was still the `change-me-to-a-long-random-string` placeholder from `.env.example`) via Python's `secrets.token_urlsafe(48)`

### Why

`/auth/login` takes a JSON body (`{"email", "password"}`) rather than FastAPI's built-in `OAuth2PasswordRequestForm` (which expects form data with a `username` field). The form-data flavor exists mainly so Swagger UI's "Authorize" button works out of the box — not needed here since the frontend will call this as a normal JSON API, and `email`/`password` field names are clearer than the OAuth2 spec's `username`/`password`.

Tests hit the real Neon dev database directly instead of a separate test database or an in-memory one. The "correct" version of this (like Spring Boot's `@Transactional` test rollback, or an H2 in-memory DB) is a reasonable amount of extra machinery for what this checkpoint needs — noted below as a known simplification, not an oversight.

### How

```
cd backend
./venv/Scripts/activate
pip install -r requirements.txt   # adds bcrypt, pyjwt, pydantic[email]
uvicorn app.main:app --reload
```

```
# signup
curl -X POST localhost:8000/auth/signup -H "Content-Type: application/json" \
  -d '{"email":"you@example.com","password":"yourpassword"}'

# login
curl -X POST localhost:8000/auth/login -H "Content-Type: application/json" \
  -d '{"email":"you@example.com","password":"yourpassword"}'
# → {"access_token": "...", "token_type": "bearer"}

# me
curl localhost:8000/auth/me -H "Authorization: Bearer <access_token>"
```

**Verification actually performed:**
- `pytest -q` → 5 passed (1 health + 4 auth)
- Full manual run against a live `uvicorn` server (not just `TestClient`): signup → real row created in Neon → login → real JWT returned → `/me` with that token → correct user returned → `/me` with no token → `401` → duplicate signup → `400`

### Notes / gotchas

- Hit a real "why is this returning 404" moment: a **stale `uvicorn` process from Checkpoint 1/2 testing was still holding port 8000** in the background, so manual `curl` checks were silently hitting the old app (without the auth routes) instead of the newly-started one. Symptom was `{"detail":"Not Found"}` on routes that definitely existed in the code. Lesson: when a manually-run server doesn't reflect a code change, check for a leftover process on the port before assuming the code is wrong.
- Known simplification: auth tests run against the real dev database with randomly generated emails, not an isolated test database. Fine for now (Neon dev DB just accumulates a handful of harmless test users); revisit with a proper test-DB/rollback pattern if it ever becomes annoying.

---

## Checkpoint 4 — Resume upload + text extraction

Done.

### Questions asked & decisions

No new architectural questions this checkpoint — this one followed the plan in ARCHITECTURE.md directly (`resume_parser` service, `POST /resumes/upload`). One implementation-level judgment call:

**Q: How to get a real test PDF with actual extractable text, without adding a PDF-generation library as a project dependency?**
Decision: temporarily installed `fpdf2` in the dev venv purely to generate one static fixture file (`backend/tests/fixtures/sample_resume.pdf`), then removed `fpdf2` again — it's not in `requirements.txt` and never will be, since nothing at runtime or in the test suite going forward needs to *generate* PDFs, only *read* them.
Why: a hand-crafted minimal PDF byte string is unreliable for testing real text extraction (many minimal/hand-written PDFs have no real text-drawing operators, so `pypdf` would extract nothing, giving a false sense of test coverage). Generating one real PDF once and committing it as a binary fixture is the same pattern as committing a sample image/CSV fixture in a Java test's `src/test/resources`.

### What was built

- `app/services/resume_parser/extract.py` — `extract_text(filename, file_bytes)`: dispatches on file extension to `pypdf` (PDF) or `python-docx` (DOCX), raises `UnsupportedFileTypeError` for anything else. Pure function, no FastAPI/DB imports — same reason you'd keep a Spring `@Service` free of `HttpServletRequest` — it's independently testable and reusable
- `app/schemas/resume.py` — `ResumeOut` DTO (`id`, `original_filename`, `extracted_text`, `uploaded_at`)
- `app/routes/resumes.py` — `POST /resumes/upload`: reads the uploaded file, rejects anything over 5MB or an empty extraction result, otherwise stores a `Resume` row linked to `current_user.id` (via `Depends(get_current_user)`, same pattern as `/auth/me`)
- `tests/test_resumes.py` — 4 tests: DOCX upload + extraction (DOCX built in-memory via `python-docx`, no fixture file needed since that library is already a real dependency), PDF upload + extraction (using the committed fixture), unsupported file type rejected, upload without auth rejected
- `tests/fixtures/sample_resume.pdf` — the one committed binary test fixture, containing real extractable text
- Added `pypdf`, `python-docx`, `python-multipart` to `requirements.txt`. (`python-multipart` isn't imported directly anywhere — FastAPI requires it installed to parse `multipart/form-data` file uploads at all; this is the same kind of "present on the classpath but never explicitly imported" dependency as a JDBC driver jar.)

### Why

File type is decided by filename extension, not the browser-supplied `Content-Type` header — the header is client-controlled and easy to get wrong or spoof, so trusting the actual bytes is only slightly better; extension is simplest and good enough for a personal tool where the user is uploading their own file. `original_filename` is stored as sent by the client (not sanitized/randomized) since it's never used as a filesystem path — it's just a DB column rendered back to the same user, so path-traversal-style risks don't apply here.

The 5MB cap and the "reject empty extraction" check both exist for the same reason: fail fast with a clear `400` at upload time instead of silently storing a useless or oversized row that breaks something two checkpoints later (Checkpoint 5's matching call would otherwise send an empty resume to OpenAI and get a meaningless score).

**Why `upload_resume` is `async def` while the auth routes are plain `def`:** not a style choice — it's forced. `UploadFile.read()` (FastAPI/Starlette's file object) is declared `async def`, so it returns a coroutine; `await` is only legal syntax inside an `async def` function. The auth routes never call anything `async`-only (SQLAlchemy's sync `Session`, `bcrypt`, `PyJWT` are all plain blocking calls), so they stay plain `def` and FastAPI quietly runs them in a worker thread pool instead.

Worth being honest about: once a route is `async def`, FastAPI runs it directly on the event loop with no automatic thread-pool offloading — but this function still calls blocking synchronous code inline (`extract_text()`'s CPU-bound PDF/DOCX parsing, and `db.commit()` on a sync SQLAlchemy session). Closest Spring equivalent: the classic WebFlux mistake of calling a blocking JDBC call inside a `Mono`/`Flux` chain without `subscribeOn(Schedulers.boundedElastic())` — it works, but it blocks the reactive thread while it runs instead of freeing it for other requests. At this project's scale (personal MVP, low concurrency) this doesn't matter in practice, but it's a real tradeoff, not an oversight — a "fully correct" version would either use plain `def` + `file.file.read()` (letting FastAPI's thread pool cover the DB call and the parsing too) or explicitly wrap the blocking calls in `run_in_threadpool`. Not worth the complexity unless it becomes an actual bottleneck.

### How

```
cd backend
./venv/Scripts/activate
pip install -r requirements.txt   # adds pypdf, python-docx, python-multipart
uvicorn app.main:app --reload
```

```
curl -X POST localhost:8000/resumes/upload \
  -H "Authorization: Bearer <access_token>" \
  -F "file=@resume.pdf;type=application/pdf"
```

**Verification actually performed:**
- `pytest -q` → 9 passed (1 health + 4 auth + 4 resumes)
- Manual run against a live `uvicorn` server: uploaded the real PDF fixture → got back real extracted text; uploaded a `.txt` file → `400`; uploaded without a token → `401`

### Notes / gotchas

- Same stale-port lesson as Checkpoint 3 — checked `netstat` for an existing listener on 8000 *before* starting a fresh server this time, instead of after getting confused by a wrong response.
- A quirk specific to this machine's git-bash `curl`, not the app: `curl -F file=@/tmp/resume.txt` failed with "Failed to open/read local data from file" using an absolute `/tmp/...` path, but the identical upload worked fine with a path relative to the current directory. Worth remembering if a manual `curl` upload mysteriously fails to even reach the server — check the path style before suspecting the API.
- Known limitation, not fixed here: a scanned/image-only PDF (no real text layer) will extract to an empty string and get a `400` — there's no OCR step. Out of scope for V1; would be a deliberate, separate feature if ever needed.

---

## Checkpoint 5 — AI match score + missing skills

**Status: done.** Live-verified against the real Gemini API — see the "Provider swap" section at the end of Checkpoint 6 for the actual live run and result.

> **Addendum — provider swapped from OpenAI to Gemini.** Everything below this line describes the checkpoint as originally built against OpenAI, kept as-is since it's an accurate record of what actually happened and why. The provider was then switched to Google Gemini — full reasoning, what changed, and what stayed the same is documented once at the end of Checkpoint 6 (rather than duplicated here and there), since both checkpoints were migrated together in a single pass.

### Original build (against OpenAI)

### Questions asked & decisions

**Q: How to actually get OpenAI's "structured outputs" (ARCHITECTURE.md's plan from Checkpoint 0) rather than prompting "return JSON" as text?**
Decision: use the OpenAI Python SDK's `client.chat.completions.parse(..., response_format=MatchResult)`, passing a **Pydantic model directly** as `response_format`.
Why: this is the current SDK's built-in way to do structured outputs — you define the shape once as a normal Pydantic model, the SDK derives the JSON schema from it automatically, sends it to the model as a hard constraint (not just a prompt suggestion), and hands back an **already-parsed, already-validated instance of that same Pydantic model** (`completion.choices[0].message.parsed`) instead of a raw string you'd have to `json.loads()` and hope is well-formed. Closest Spring analogy: like using a `@JsonSchema`-validated deserializer instead of manually parsing a `String` response body — the validation and the model are the same object, not two things you keep in sync by hand.

**Q: How to test this without hitting the real OpenAI API on every `pytest` run?**
Decision: `score_resume_against_job()` (the one function that calls OpenAI) is monkeypatched in `tests/test_matches.py` to return a fixed `MatchResult`, so the test suite verifies all the *route* logic — DB writes, ownership checks, auth, response shape — without any network call, cost, or need for a real API key.
Why: unlike the Neon DB (free, already live, fine to hit on every test run), a real OpenAI call costs real money per request and shouldn't fire dozens of times on every `pytest` invocation or in CI. This is the same reasoning as mocking a payment gateway or third-party email service in a Spring test with Mockito — the boundary you don't own or want to actually invoke in a unit test gets swapped for a fake, and the code under test (your own route logic) still runs for real. A genuine, unmocked, real-API call is still done manually once (see "How" below) to prove the actual integration works, not just the mocked logic around it.

### What was built

- `app/services/ai_engine/schemas.py` — `MatchResult` (`score: int` 0–100, `missing_skills: list[str]`, `strengths: list[str]`) — this one Pydantic model is *both* the structured-output contract sent to OpenAI *and* the return type of the service function. One definition, not two.
- `app/services/ai_engine/prompts.py` — `build_match_messages()`: builds the system + user messages from resume text + JD text. Kept as its own function/file (per ARCHITECTURE.md's original plan) so the prompt is reviewable and testable on its own, not buried inside a route handler.
- `app/services/ai_engine/client.py` — `get_openai_client()` (lazily creates one `OpenAI` client, reused across calls — analogous to a singleton-scoped Spring `@Bean` for an HTTP client, not re-created per request) and `score_resume_against_job()`, the function that actually calls `.parse()` and returns a `MatchResult`.
- `app/schemas/match.py` — `MatchCreate` (`resume_id`, `job_description_text`), `MatchOut` (full `Match` row shape including `cover_letter: str | None`, still always `null` until Checkpoint 6)
- `app/routes/matches.py` — `POST /matches`: loads the resume, **checks it belongs to the calling user** (404 if not — a different user's resume ID should look exactly like a nonexistent one, not leak that it exists), stores the job description, calls the AI engine, stores and returns the `Match`. Wraps the OpenAI call in a `try/except` → clean `502` instead of a raw stack trace if the API call fails (network issue, bad key, rate limit, etc.) — this is a genuine system boundary (an external, unreliable API), which is exactly the kind of place error handling belongs, unlike defensive checks against your own internal code.
- `tests/test_matches.py` — 4 tests, all using the mocked `score_resume_against_job`: successful match creation (verifies the mocked score/skills/strengths round-trip correctly through the DB and back out), someone else's resume rejected (`404`), nonexistent resume rejected (`404`), no auth rejected (`401`)
- Added `openai` to `requirements.txt`

### Why

`resume_id` + a raw job description string (not a `job_description_id`) is the request shape for `POST /matches`, because the frontend will always have just gotten a JD as pasted text from the user — the backend creates the `job_descriptions` row itself as part of handling the match, rather than requiring the client to make two separate calls (create JD, then create match). One request, matching how ARCHITECTURE.md's flow was already sketched.

The ownership check (`resume.user_id != current_user.id` → `404`, not `403`) is deliberate: a `403` would confirm "this resume exists, you're just not allowed to see it," which leaks information about other users' data. `404` makes "not yours" and "doesn't exist" indistinguishable from the outside — same reasoning as the signup/login error messages from Checkpoint 3.

### How

```
cd backend
./venv/Scripts/activate
pip install -r requirements.txt   # adds openai
# put a real OPENAI_API_KEY in .env, then:
uvicorn app.main:app --reload
```

```
curl -X POST localhost:8000/matches \
  -H "Authorization: Bearer <access_token>" \
  -H "Content-Type: application/json" \
  -d '{"resume_id": 1, "job_description_text": "Looking for a backend engineer with Python and Postgres experience."}'
```

**Verification actually performed:**
- `pytest -q` → 13 passed (9 previous + 4 new match tests), all without a real API key since the OpenAI call is mocked
- **Not yet performed**: a real, unmocked call to OpenAI's API. Blocked on a real `OPENAI_API_KEY` — once added, the plan is to re-run the `curl` above against a live server and confirm a real `MatchResult` (real score, real skills, not the mocked `72`/`Kubernetes`/`GraphQL` fixture values) comes back and lands correctly in Postgres.

### Notes / gotchas

- Confirmed this SDK version (`openai==2.44.0`) has `client.chat.completions.parse` available as a first-class (non-`beta`) method before building around it — worth checking on any future upgrade, since OpenAI has moved this API around between SDK versions.
- This checkpoint is **not** marked done in ROADMAP.md yet — code-complete and unit-tested, but "done" per this project's own standard means a real end-to-end run against the live dependency (same bar Checkpoints 2 and 3 held for Postgres/JWT), and that step is still pending the real API key.

---

## Checkpoint 6 — Cover letter generation

**Status: done.** Built alongside Checkpoint 5 rather than waiting, since both needed the same missing API key. Live-verified against the real Gemini API — see "Provider swap" below.

> **Addendum — provider swapped from OpenAI to Gemini.** Same as Checkpoint 5: the section below describes the original build against OpenAI as it actually happened. See "Provider swap" at the end of this checkpoint for what changed.

### Original build (against OpenAI)

### Questions asked & decisions

**Q: Should the cover letter also use structured outputs (`response_format=<PydanticModel>`), even though it's really just one text field?**
Decision: yes — added `CoverLetterResult(BaseModel): cover_letter: str` and used `.parse()`, same as `MatchResult`.
Why: ARCHITECTURE.md's design principle #2 ("all OpenAI calls that feed application logic use structured output") doesn't carve out an exception for single-field responses. Concretely, it also avoids a real failure mode of plain free-text prompting: models often prepend chatty preambles ("Sure, here's a tailored cover letter:") or wrap the letter in markdown/quotes even when told not to — constraining the response to a schema with exactly one field (`cover_letter`) is more reliable than trying to string-strip an unpredictable preamble afterward.

**Q: New endpoint, or extend `POST /matches`?**
Decision: new endpoint, `POST /matches/{match_id}/cover-letter`, matching ARCHITECTURE.md's original flow sketch (`4. POST /matches/{id}/cover-letter`).
Why: generating a cover letter is a deliberate, separate user action *after* seeing the match score (see FLOWS.md's Flow 3 user steps) — combining it into `POST /matches` would force every match to pay for a cover-letter generation call even when the user just wants to see the score first.

### What was built

- `app/services/ai_engine/schemas.py` — added `CoverLetterResult`
- `app/services/ai_engine/prompts.py` — added `build_cover_letter_messages()` + its own system prompt, instructing the model to reference only real resume content (no invented experience) and to return just the letter body, no placeholder brackets like `[Company Name]`
- `app/services/ai_engine/client.py` — added `generate_cover_letter()`, same `.parse()` pattern as `score_resume_against_job()`
- `app/routes/matches.py` — added `POST /matches/{match_id}/cover-letter`: loads the `Match`, checks ownership **through the resume relationship** (`match.resume.user_id != current_user.id`, since `Match` has no `user_id` column of its own — ownership is inherited via `resume_id → resumes.user_id`), calls the AI engine using `match.resume.extracted_text` + `match.job_description.raw_text`, updates `match.cover_letter` in place, returns the full updated `MatchOut`. No new response schema needed — `MatchOut` already had a `cover_letter` field (always `null` until now)
- `tests/test_matches.py` — extended with a `mock_ai_cover_letter` fixture and 4 new tests: successful generation, someone else's match rejected (`404`), nonexistent match rejected (`404`), no auth rejected (`401`)

### Why

Reusing `MatchOut` as the response model (instead of a narrower `CoverLetterOut`) means the frontend gets the whole match back — score, skills, strengths, *and* the new cover letter — in one response, so it doesn't need to re-fetch or merge two separate objects to render the full result page.

The ownership check pattern is identical to `POST /matches`'s (`404`, not `403`, for "not yours" — see Checkpoint 5's reasoning) but one level deeper: it walks `match → resume → user_id` instead of checking a direct column, since `matches` doesn't have its own `user_id`. This is a direct consequence of the schema design from Checkpoint 2 — ownership always flows from `resumes`, the same way a Hibernate `@ManyToOne` chain lets you write `match.getResume().getUser().getId()` instead of denormalizing a `userId` onto every child table.

### How

```
curl -X POST localhost:8000/matches/1/cover-letter \
  -H "Authorization: Bearer <access_token>"
```

**Verification actually performed:**
- `pytest -q` → 17 passed (13 previous + 4 new cover-letter tests), all mocked, no real API key needed
- **Not yet performed**: a real OpenAI call. Once a real `OPENAI_API_KEY` is added, plan is to run both this and Checkpoint 5's `curl` back to back against a live server (create a real match, then generate a real cover letter from it) and confirm both land correctly in Postgres — at that point both checkpoints get checked off in ROADMAP.md together.

### Notes / gotchas

- Same as Checkpoint 5: not marked done in ROADMAP.md until a real, live API call has actually been run — code-complete and fully unit-tested isn't the same bar this project holds for "done."

---

## Provider swap — OpenAI → Google Gemini (applies to Checkpoints 5 and 6)

**Status: code complete; live verification still pending** (now blocked on a real `GEMINI_API_KEY` instead of `OPENAI_API_KEY`).

### Questions asked & decisions

**Q: OpenAI now requires a payment method on file (no free tier for new accounts) — what are the free alternatives?**
Three real options were weighed:
- **Google Gemini (free tier via Google AI Studio)** — no card required, generous free rate limits, and its `google-genai` SDK supports the same "constrain the response to a Pydantic schema" structured-output pattern already in use. Chosen.
- **Groq (free tier, OpenAI-API-compatible)** — free, very fast inference on open models, and its API mimics OpenAI's closely enough that much of the existing code might have worked with just a `base_url` change. Not chosen: its structured-output support wasn't confirmed to match OpenAI's `.parse()` convenience method exactly, and verifying that felt riskier than just switching SDKs outright.
- **Local model via Ollama** — the only truly free-forever option, but directly contradicts this project's own stated principle from Checkpoint 0 ("do not start with local LLMs initially"). Not chosen; noted as a fallback if API costs ever become a real problem later.

Before committing to Gemini, its structured-output API was verified empirically (not assumed from memory) — installed `google-genai`, introspected `types.GenerateContentConfig` and `types.GenerateContentResponse` directly in a Python shell, and confirmed `response_schema` accepts a Pydantic class and the response object exposes an already-parsed `.parsed` attribute, the same shape as OpenAI's. Same "verify before building around it" habit as checking `openai.chat.completions.parse` existed before Checkpoint 5 was first built.

### What changed vs. what didn't

**Changed:**
- `app/config.py` — `openai_api_key`/`openai_model` → `gemini_api_key`/`gemini_model` (default `gemini-2.0-flash`)
- `app/services/ai_engine/client.py` — rewritten to use `google.genai.Client(...).models.generate_content(..., config=types.GenerateContentConfig(system_instruction=..., response_mime_type="application/json", response_schema=MatchResult))` instead of OpenAI's `client.chat.completions.parse(...)`
- `app/services/ai_engine/prompts.py` — `build_match_messages`/`build_cover_letter_messages` (OpenAI's list-of-role-dicts shape) became `build_match_prompt`/`build_cover_letter_prompt` (plain string), since Gemini takes the system prompt separately via `system_instruction` rather than as a message in the same list
- `requirements.txt` — `openai` → `google-genai`
- Root `.env.example` and `backend/.env` — `OPENAI_API_KEY`/`OPENAI_MODEL` → `GEMINI_API_KEY`/`GEMINI_MODEL`
- README/ARCHITECTURE/FLOWS/API docs — updated to say Gemini throughout (all "current state" docs); this file's Checkpoint 5/6 narrative was deliberately left describing OpenAI, since that's what was actually built and decided at the time

**Did not change:**
- `app/services/ai_engine/schemas.py` (`MatchResult`, `CoverLetterResult`) — the actual data contracts
- `app/routes/matches.py` — imports `score_resume_against_job`/`generate_cover_letter` by name and never touches the OpenAI/Gemini SDK directly
- `tests/test_matches.py` — monkeypatches those same two function names at the route-module level, so the tests never knew or cared which provider was behind them

This is the payoff of the Checkpoint 5 decision to keep `MatchResult`/`CoverLetterResult` as the one shared contract between the AI layer and everything downstream: swapping the entire AI provider touched exactly 5 files (`config.py`, `client.py`, `prompts.py`, `requirements.txt`, env files) and **zero** route, schema, or test files. Closest Spring analogy: this is what depending on an interface instead of a concrete class buys you — swap the `@Service` implementation, the `@Autowired` call sites and their tests never notice.

### Verification actually performed

- `pytest -q` → still 17 passed after the swap, unchanged — confirms the mocked route-level tests are genuinely provider-agnostic, not incidentally coupled to OpenAI
- **Live end-to-end run against the real Gemini API** (model: `gemini-2.5-flash`, upgraded from the initial `gemini-2.0-flash` default at the user's request once a real key was available): signed up a fresh user, uploaded the real PDF fixture (resume text: "...skilled in Java, Spring Boot, and MySQL"), then called `POST /matches` against a job description asking for "Java, Spring Boot, and relational databases like MySQL or PostgreSQL. Kubernetes and GraphQL experience is a plus." Real, non-mocked Gemini response:
  ```json
  {
    "score": 85,
    "missing_skills": ["Kubernetes", "GraphQL"],
    "strengths": ["Experience in Java", "Experience in Spring Boot", "Experience in MySQL"]
  }
  ```
  Correctly identified every real skill actually present in the resume text and every real gap actually present in the JD — not hallucinated. Then called `POST /matches/{id}/cover-letter` on that same match; Gemini returned a coherent, on-topic cover letter body referencing only real resume content (Java, Spring Boot, MySQL), with no invented experience, no placeholder brackets, and no chatty preamble — exactly what the system prompts asked for.
  Queried Postgres directly afterward (`SessionLocal()` + `db.get(Match, id)`) to confirm the real score/skills/cover letter actually persisted, not just that the HTTP response looked right.
- Both checkpoints checked off in ROADMAP.md following this run.

### Notes / gotchas

- `google-genai`'s `GenerateContentConfig` fields are camelCase internally (`responseSchema`, `responseMimeType`) but the Pydantic model accepts snake_case kwargs via alias — `inspect.signature()` on the class showed the camelCase names and briefly looked like snake_case wasn't supported; actually constructing it with snake_case kwargs confirmed it works. Lesson: `inspect.signature` on a Pydantic model with aliases doesn't tell the whole story — try the actual call, don't just read the signature.
- Gemini's `response.parsed` can be `None` (or occasionally not an instance of the requested class) if the model doesn't comply with the schema — narrowed the check from `is None` to `isinstance(result, MatchResult)` / `isinstance(result, CoverLetterResult)` for a stricter guarantee.
- Same stale-port lesson as Checkpoints 3 and 4, paid off again: checked `netstat` and killed a leftover `uvicorn` process on port 8000 *before* starting the live-verification server this time, instead of after getting confused by it.

---

## Test data cleanup (retroactive fix, applies to Checkpoints 3-6)

**Status: done.**

### Questions asked & decisions

**Q: Tests hit the real dev Neon DB and never delete anything — is that a problem?**
It had already become one: by this point, 3 test files × repeated runs had left **83 stale test users** (plus their resumes/job_descriptions/matches) sitting in the dev database. This was flagged as a known, deferred simplification back in Checkpoint 3 ("revisit with a proper test-DB/rollback pattern if it ever becomes annoying") — it became annoying.

Two real options:
- **Transaction rollback per test** (the Spring `@Transactional` test equivalent) — the "textbook correct" answer, but requires SQLAlchemy's SAVEPOINT-plus-event-listener pattern to work around route code calling `db.commit()` explicitly. Rejected as more complexity than this project needs.
- **Watermark-based cleanup fixture** — record the highest `users.id` before each test, delete every user above that watermark after. Chosen: much simpler to read and reason about, and combined with cascade deletes (see below), one `DELETE` statement cleans up everything a test created regardless of which tables it touched.

### What was built

- **Added `ondelete="CASCADE"` to every child foreign key** (`resumes.user_id`, `job_descriptions.user_id`, `matches.resume_id`, `matches.job_description_id`) via a new Alembic migration. This isn't just a test-cleanup hack — it's the correct real-world behavior too: deleting a user's account should delete their resumes, job descriptions, and matches with it, not leave orphaned rows. Closest Java analogy: `@OneToMany(cascade = CascadeType.REMOVE)` on the parent side of a JPA relationship.
- **`backend/tests/conftest.py`** — one `autouse=True` pytest fixture (`cleanup_test_data`) that runs around every test: records `MAX(users.id)` before, deletes every user above that watermark after. No test file needs to opt in or know it exists.
- **One-time manual cleanup** of the 83 pre-existing stale rows (and everything they cascaded to) before verifying the new fixture.

### Why

The watermark approach was chosen over deleting by email pattern (e.g. `WHERE email LIKE '%test%'`) because it doesn't depend on every test remembering to use a recognizable email — it catches *anything* created during the test, including future tests that don't follow today's naming convention.

### How

```
cd backend
alembic upgrade head   # applies the CASCADE migration
pytest -q
```
No test needs to change or explicitly request the fixture — `conftest.py` fixtures with `autouse=True` apply automatically to every test in the directory, the same way a Spring `@BeforeEach`/`@AfterEach` in a shared base test class applies to every subclass without each test re-declaring it.

**Verification actually performed:**
- Confirmed the DB had 83 stale users before the fix; ran `DELETE FROM users WHERE email LIKE '%test%' OR email LIKE '%example.com'` once manually and confirmed cascades correctly zeroed out `resumes`, `job_descriptions`, and `matches` too (proving the new FK constraints work, not just that a row disappeared)
- Ran `pytest -q` (17 passed) **twice in a row**, checking real row counts via a direct DB query after each run: `0/0/0/0` both times — not just "tests passed" but confirmed the dev DB actually stays empty across repeated runs, which is the actual thing being fixed

### Notes / gotchas

- This also quietly cleaned up my own manual `curl`-based live-verification users from earlier (e.g. `live-gemini-check@example.com`) since they matched the same email pattern during the one-time manual cleanup — no loss, since the actual verified output (score, skills, cover letter text) is already preserved in this doc rather than needing to live in the DB as proof.

---

## Checkpoint 7 — Wire up frontend flows

**Status: done**, with one honest caveat on verification depth — see "Notes / gotchas."

### Questions asked & decisions

No new architectural questions — this checkpoint largely followed ARCHITECTURE.md's plan directly (React SPA calling the REST API, JWT held client-side). The real decisions were implementation-level:

**Q: How much app state management is actually needed?**
Decision: plain React `useState` + one `AuthContext` (React Context, no Redux/Zustand/TanStack Query).
Why: the whole app is 4 pages and one linear flow (upload → JD → match → cover letter) held in local component state on `Dashboard`. A state management library solves problems (shared cross-page state, caching, background refetching) this app doesn't have yet. Auth is the one piece of state genuinely needed in multiple places (every page needs to know "is someone logged in"), which is exactly what Context is for — reaching for a bigger tool here would be solving a problem that doesn't exist.

**Q: One page per step, or one page holding the whole flow?**
Decision: one `Dashboard` page that conditionally renders upload form → JD form → results, based on what's in state (`resume`, `match`).
Why: matches the actual user flow documented in FLOWS.md — it's one continuous task ("go from resume to cover letter"), not several independent destinations a user would navigate between and back. Fewer pages/routes to wire up, and no risk of losing the just-uploaded resume by navigating away from it.

### What was built

- **`lib/api.ts`** — typed client functions for every backend endpoint (`signup`, `login`, `getMe`, `uploadResume`, `createMatch`, `generateCoverLetter`), plus `UserOut`/`Token`/`ResumeOut`/`MatchOut` TypeScript interfaces copied field-for-field from API.md's documented response shapes (not guessed) — a shared `request()` helper attaches the `Authorization` header when a token is passed and throws a typed `ApiError` (with the real HTTP status) on any non-2xx response, parsing the backend's `{"detail": "..."}` error body into the thrown error's message
- **`context/AuthContext.tsx`** — holds `token`/`user` in React state, persists the token in `localStorage`, restores it on page load and re-validates it via `GET /auth/me`
- **`components/ProtectedRoute.tsx`** — layout route that redirects to `/login` if there's no valid token, otherwise renders its nested route (`Dashboard`)
- **`pages/Login.tsx`**, **`pages/Signup.tsx`** — plain forms calling `useAuth().login()`/`.signup()`, redirect to `/dashboard` on success, show the thrown `ApiError`'s message on failure
- **`pages/Dashboard.tsx`** — the whole upload → JD → match → cover-letter flow in one page, conditionally rendered based on `resume`/`match` state; includes a "Copy" button on the generated cover letter (`navigator.clipboard.writeText`)
- **`pages/Root.tsx`** — replaces Checkpoint 1's `Home.tsx` (whose only job, a health-check smoke test, was already superseded); redirects `/` to `/dashboard` or `/login` depending on auth state
- **`router.tsx`** — updated to the real route table (`/`, `/login`, `/signup`, and `/dashboard` behind `ProtectedRoute`)
- **`main.tsx`** — wraps `RouterProvider` in `AuthProvider` so every routed page can call `useAuth()`

### Why

`getMe()`/`.parsed`-style DTOs are hand-written TypeScript interfaces rather than generated from the backend's OpenAPI schema (FastAPI auto-generates one at `/openapi.json`). Codegen (e.g. `openapi-typescript`) would remove the risk of the two sides drifting apart, but it's another tool/build step to learn and maintain for 4 response shapes that change rarely — hand-written interfaces checked directly against API.md were judged good enough for this project's size. Worth revisiting with codegen if the API surface grows a lot in V2/V3.

The frontend's `ProtectedRoute` check is explicitly **not** a security boundary, just a UX one — see the new "How the frontend half actually works" note added to FLOWS.md's Flow 1. Every real protection (can't see someone else's resume/match, can't call the API without a valid JWT) still lives entirely in the backend, same as before this checkpoint.

### How

```
# backend
cd backend && ./venv/Scripts/activate && uvicorn app.main:app --reload

# frontend (separate terminal)
cd frontend && npm run dev
```
Then open `http://localhost:5173` — should redirect to `/login` if logged out, or `/dashboard` if a valid token is already in `localStorage`.

**Verification actually performed:**
- `npm run build` (`tsc -b && vite build`) → type-checks and builds cleanly, both before and after adding the copy button
- Booted both dev servers together (checked `netstat` for stale processes on 8000/5173 first, killed 3 leftover ones — same lesson as every prior checkpoint), confirmed the backend `/health` and the frontend's SPA shell both respond, and that `/login`/`/dashboard` correctly serve the SPA fallback (client-side routing) rather than 404ing
- Every request shape in `lib/api.ts` was checked field-for-field against API.md's documented contracts (the same contracts the backend's real, passing test suite enforces) rather than assumed from memory

### Notes / gotchas

- **Honest limitation: no browser automation tool is available in this environment** (no Playwright/Puppeteer/similar), so the actual click-through (sign up → upload a real file → paste a JD → see a real score → generate a real cover letter, all via mouse/keyboard in a rendered browser) has **not** been performed by me. What has been verified: the code type-checks and builds without error, the dev servers boot and serve the right shell/routes, and every API call's request/response shape is traced against the backend's documented (and independently live-tested) contracts. This is a meaningfully lower bar than actually watching the UI work. **Recommend doing one manual pass yourself** — `npm run dev` in `frontend/`, `uvicorn app.main:app --reload` in `backend/`, open `localhost:5173`, and walk through sign up → upload → match → cover letter once — before considering this checkpoint fully trustworthy.
- Deleted `pages/Home.tsx` (Checkpoint 1's health-check landing page) rather than keeping it around unused — its one job was already done and documented; `getHealth()` stays in `lib/api.ts` as a small unused-for-now utility rather than being deleted, since a health check is still a reasonable thing to want later (e.g. a status indicator).

---

## UX polish — content and visual design (post-Checkpoint-7, user feedback)

**Status: done.**

### Questions asked & decisions

User feedback after trying the real app: the skill-gap section was "boring and long," the cover letter was "long and nobody will read it like this," and the overall UI needed polish. Two different kinds of problem, fixed at two different layers:

**Q: Is the "boring and long" problem a content problem or a display problem?**
Both, and both needed fixing:
- **Content**: the AI's own output was verbose — `strengths`/`missing_skills` were full phrases ("Experience in Java") instead of short tags, and the cover letter was a conventional multi-paragraph letter. No amount of frontend styling fixes verbose source data.
- **Display**: even short data was rendered as plain bullet-point text lists with no visual hierarchy — nothing pulled the eye to the one number (the score) that matters most.

**Q: How should the redesign choose colors, rather than picking hexes by eye?**
Used the `dataviz` skill's validated reference palette instead of inventing colors — it provides a pre-validated status palette (good/warning/critical) with documented contrast numbers, a categorical palette (used for the accent blue), and explicit guidance that status color must never carry meaning alone (icon + label + color, never color-only). One gap found and fixed rather than shipped: the skill's own reference palette flags `warning` (`#fab219`) as failing text contrast on a light surface (1.79, needs ≥4.5) — verified this by computing the actual WCAG contrast ratio (see "How" below) rather than eyeballing it, and derived a same-hue-family darker substitute (`#8a5a00`, 5.77 contrast) for use as *text*, while the original bright hex stays valid for non-text uses like the meter fill. Found and fixed a second one myself, same method: the accent blue as literal button-background-with-white-text failed too (4.42, just under 4.5) — swapped to the palette's own darker blue step (`#1c5cab`, 6.63 contrast) for button backgrounds specifically, keeping the lighter blue for non-text uses like focus rings.

### What was built

**Backend** — `app/services/ai_engine/prompts.py`:
- `SYSTEM_PROMPT` now explicitly requires `missing_skills`/`strengths` to be short skill/technology names (1-3 words, e.g. `"Kubernetes"` not `"Experience in Kubernetes"`), capped at 5 items per list
- `COVER_LETTER_SYSTEM_PROMPT` rewritten to require a fixed short structure: one opening sentence, 3-4 concise bullet points (`"- "` prefix, under 20 words each) highlighting specific real resume/JD matches, one closing sentence — instead of an open-ended "write a cover letter"

**Frontend** — new components, all in `frontend/src/components/`:
- `ScoreMeter.tsx` — the score as a large proportional number + a short status label ("Strong match"/"Partial match"/"Weak match") + a horizontal meter bar, color banded by score (≥75 good/green, ≥50 warning/amber, else critical/red) — the dataviz skill's "Meter" pattern (fill carries severity, track is a neutral gray) rather than a plain number
- `SkillChip.tsx` — renders each strength/missing-skill as a small pill: colored wash background + accessible-contrast text + a small inline SVG check/X icon, so status is never color-alone (icon + label + color, all three)
- `CoverLetterView.tsx` — parses the cover letter's `"- "`-prefixed lines into a real bulleted list and renders the opening/closing sentences as short paragraphs, instead of dumping the whole string into one `<p>`
- `index.css` — the dataviz skill's palette values (page/surface two-tone, status good/warning/critical + accessible text variants, categorical blue as the accent) defined once as CSS custom properties, with a `prefers-color-scheme: dark` block using mode-appropriate steps (not just a flipped light palette) — referenced everywhere via Tailwind's arbitrary-value syntax (`bg-[var(--surface)]`) rather than hardcoded hex scattered across components
- `Dashboard.tsx`, `Login.tsx`, `Signup.tsx` — restyled onto the new tokens: card surfaces with a two-tone page/card background instead of flat white, consistent rounded-xl cards with a subtle shadow, and a real accent-colored primary button instead of plain black

### Why

Kept `missing_skills`/`strengths` as `list[str]` (no schema change) and `cover_letter` as a single `str` (no schema change) — the conciseness fix lives entirely in the *prompt*, not the data contract. This meant zero backend route/schema/migration changes and zero test changes; only `prompts.py` changed, same "swap the implementation behind a stable contract" pattern as the OpenAI→Gemini provider swap.

Colors were chosen in the order the dataviz skill prescribes — form first (meter for a bounded 0-100 score, chips for discrete status items), then color assigned by job (status color for pass/fail-flavored items, categorical for the one-off accent), validated last. Not the reverse (pick nice-looking hexes, hope they're accessible).

### How

```
# backend: no new dependencies, just prompt text changed
cd backend && uvicorn app.main:app --reload

# frontend
cd frontend && npm run dev
```

**Verification actually performed:**
- Computed real WCAG contrast ratios (relative-luminance formula, not eyeballed) for every text-on-background color pairing introduced — caught and fixed two failures before shipping (warning-as-text, accent-as-button-text) rather than after
- Ran the dataviz skill's own `validate_palette.js` against the status+accent hex set — confirmed it flags exactly the known, already-mitigated issue (`warning` sub-3:1, "ship visible labels" — which is exactly what the icon+label chip design already does) and nothing new
- `npm run build` → type-checks and builds cleanly after every change
- **Live run against the real Gemini API** with the updated prompts: `strengths` came back as `["Java", "Spring Boot", "MySQL"]` and `missing_skills` as `["Kubernetes", "GraphQL", "Docker"]` — real short tags, not sentences. Cover letter came back in the exact requested shape (1 opening sentence + 3 bullets + 1 closing sentence)
- Fed that real (not fabricated) Gemini cover-letter output through `CoverLetterView`'s actual parsing logic (via a standalone Node script running the identical split/filter/prefix-check code) and confirmed it splits into exactly 1 opening paragraph, 3 bullets, 1 closing paragraph — the real data, not a hand-crafted test string, round-trips through the real parser correctly
- Queried Postgres directly afterward to confirm the short-tag data actually persisted, then deleted that manual verification user (same cleanup discipline as every other manual check)

### Notes / gotchas

- Same honest limitation as Checkpoint 7: no browser automation tool available, so the actual rendered chips/meter/bullets have not been visually confirmed by me in a real browser — verified the data reaching each component is correct and each component's logic is correct in isolation (parsing script, build/type-check), which is strong but not the same as seeing the pixels. Worth a manual look once you get a chance.
- The "warning" band (50-74 score) is defined but hasn't come up in any real test run yet (both live runs scored 85, "good" band) — the color/label logic was verified by reading the code and running the contrast numbers, not by triggering it end-to-end with a real low-scoring resume/JD pair.
